<!Doctype html>
<html>
<head>
    <title>{{title}}</title>
    <link rel="icon" href="/img/weather.png">
    <link rel="stylesheet" href="/css/styles.css" >
</head>

    <body>
        <div class="main-content">
            {{>header}}
            <h1>{{errorMsg}}</h1>
        </div>
    <p>{{errorMsg}}</p>
    {{>footer}}
</body>
</html>
